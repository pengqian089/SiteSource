@using Dpz.Core.Web.Library.Api.Service
@inject ToolService ToolService;
@{
    Layout = null;

    var key = $"static-not-found:{Program.Version}";
    var content = await ToolService.GetStaticContentAsync(key, "reject.html");
}

@Html.Raw(content)